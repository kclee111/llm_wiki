## 코드 메모: LLM Wiki 아키텍처 개요 (architecture catch-up)

크로스플랫폼 **Tauri 2 데스크톱 앱**. LLM이 사용자 문서를 읽어 구조화된 위키 페이지를 자동 생성·유지 (Karpathy "LLM Wiki" 패턴). — `README.md:7-9`, `src-tauri/Cargo.toml:3`

### 큰 그림
- 프론트엔드 `src/` (React 19 + TS + Vite) ↔ 백엔드 `src-tauri/` (Rust + Tauri)
- 둘 사이는 **Tauri IPC (`invoke`)**, JSON 전용 → 바이너리(이미지)는 base64 (`write_file_base64` / `read_file_as_base64`)
- 백엔드는 **로컬 HTTP API**도 노출 → `mcp-server/`(독립 TS MCP 서버)와 AI 에이전트 스킬이 소비 — `README.md:46`
- `extension/` = Chrome 웹 클리퍼

### 진입점
- 프론트: `index.html:11` -> `src/main.tsx` -> `src/App.tsx:25` `function App()`. 마운트 시 `setupAutoSave()` + `startClipWatcher()` 1회 — `src/App.tsx:36-39`
- 백엔드: `src-tauri/src/main.rs:5` -> `src-tauri/src/lib.rs:136` `pub fn run()`
  - `clip_server::start_clip_server()` 최초 기동 — `lib.rs:137`
  - 플러그인 등록(opener/dialog/store/autostart/http) — `lib.rs:140-151`
  - `.setup()`: proxy env 적용 -> CLI/file-sync 상태 manage -> **api_server 시작(`lib.rs:187`)** -> tray 생성 — `lib.rs:152-204`
  - IPC 커맨드 ~50개 등록 — `lib.rs:205-260`

### 핵심 의존성 (왜 쓰는지)
- **tauri 2** — 데스크톱 셸 + IPC
- **lancedb + arrow** (`Cargo.toml:38-41`) — 임베디드 벡터 검색 (빌드에 **protoc** 필요)
- **pdfium-render** (`Cargo.toml:29`) — PDF 파싱/이미지 추출. 네이티브 lib는 `src-tauri/pdfium/`
- **reqwest(rustls)** — Rust 측 아웃바운드 fetch. CORS로 막히는 LLM API를 webview 우회 — `lib.rs:147-151`
- **calamine / docx-rs / office_oxide** — Office 문서 파싱
- 프론트: milkdown(에디터), sigma+graphology(지식 그래프, Louvain), zustand(상태), i18next(en/zh)

### 불변식
- **모든 Rust 커맨드는 `run_guarded(...)`로 감싼다** (`src-tauri/src/panic_guard.rs`) — 서드파티 파서 패닉을 `Result::Err`로 전환해 단일 파일 손상이 앱을 죽이지 않게 함. 새 커맨드도 이 패턴 준수.

### 실행 명령
- `npm run dev` (Vite, 포트 1420 고정) / `npm run tauri dev` (전체 앱)
- `npm run build` (typecheck + vite build) / `npm test` (test:mocks + test:llm)
- `cargo build` (`src-tauri/` 내, protoc 필요)

### 아직 확인 못 한 부분
- `src/main.tsx` 본문, ingest 파이프라인 단계별 흐름(`src/lib/ingest*.ts`), `api_server.rs`/`clip_server.rs` 라우트 정의

### 다음 추천
- feature-trace: "문서 ingest 흐름" — `src/lib/ingest.ts` 기점으로 임포트 -> ingest-queue -> LLM 분석 -> 위키 페이지 생성 콜그래프 추적

## feature-trace 메모: 문서 ingest 흐름

진입점은 3갈래, 모두 직렬 큐에 적재된다.

### 진입점
- UI 수동 임포트: `src/components/sources/sources-view.tsx:251` `enqueueSourceIngest(...)`
- 폴더/스케줄 임포트: `src/lib/source-lifecycle.ts:150` `enqueueSourceIngest` -> `enqueueBatch`
- Chrome 웹 클리퍼 자동: `src/lib/clip-watcher.ts:45` `enqueueIngest` (마운트 시 `startClipWatcher()` 폴링)
- 셋 다 활성 프로젝트 아니면 throw -> `src/lib/ingest-queue.ts:191-195`

### 콜 그래프
```
enqueueIngest (ingest-queue.ts:186)
 -> upsertQueuedIngestTask        (메모리 큐 삽입, dedup)
 -> saveQueue                     [.llm-wiki/ingest-queue.json 영속화]
 -> processNext (ingest-queue.ts:514)  [processing 플래그로 1개씩 직렬]
    -> getProjectPathById / hasUsableLlm
    -> autoIngest (ingest.ts:472) -> withProjectLock -> autoIngestImpl (ingest.ts:484) ★
    -> 성공: 큐 제거 / 실패: retryCount++ , MAX_RETRIES=3 도달 시 failed 아니면 pending
    -> processNext(재귀), 큐 비면 onQueueDrained (review sweep)
```

### autoIngestImpl 단계 (2-step CoT, ingest.ts:484-1160)
- MinerU 전처리(PDF+token) -> 실패 시 pdfium 폴백
- 소스 + schema/purpose/index/overview 병렬 read
- checkIngestCache ★ HIT면 이미지 캐스케이드만 돌리고 early-return
- Step 0.5 extractAndSaveSourceImages -> wiki/media/<slug>/
- Step 0.6 captionMarkdownImages (VLM, SHA-256 캡션 캐시; multimodal OFF면 ref strip)
- (긴 소스) analyzeLongSourceInChunks
- Step 1 분석: streamChat(buildAnalysisPrompt) temp=0.1 max_tokens=4096
- Step 2 생성: streamChat(buildGenerationPrompt) 출력은 `---FILE:` 블록 강제
- (조건부) 리뷰: streamChat(buildReviewSuggestionPrompt)
- Step 3 쓰기: writeFileBlocks -> writtenPaths/warnings/hardFailures (+집계파일 repair)
- Step 3.5 injectImagesIntoSourceSummary (multimodal ON)
- Step 4 parseReviewBlocks -> reviewStore.addItems
- Step 5 saveIngestCache (hardFailures==0일 때만 — 부분실패 동결 방지)
- Step 6 embedPage (embeddingConfig.enabled; index/log/overview 제외)

### 핵심 분기/에러
- 캐시 HIT -> LLM 전체 스킵, 이미지 캐스케이드만(멱등)
- abort -> 빈 결과 -> writtenPaths.length===0 -> processNext가 "성공 위장" 막고 retry
- 분석/생성 스트림 에러 -> throw (silent return [] 금지) -> retry/failed
- MinerU/캡션 실패는 치명적 아님

### 부수효과
- 디스크: wiki/**, wiki/sources/<slug>.md, wiki/media/<slug>/, .llm-wiki/ingest-queue.json, .cache/<file>.txt, image-caption-cache.json
- 외부: LLM streamChat(분석/생성/리뷰/캡션), MinerU API, 임베딩 엔드포인트
- 전역 상태: activityStore / reviewStore / wikiStore.setFileTree+bumpDataVersion

## feature-trace 메모: writeFileBlocks 파서

진입점: autoIngestImpl Step 2 생성 LLM 출력 텍스트 -> `src/lib/ingest.ts:1513` `writeFileBlocks(...)` -> `{writtenPaths, warnings, hardFailures}`.

### 콜 그래프
```
writeFileBlocks (ingest.ts:1513)
 ├ parseFileBlocks(text) (ingest.ts:357)  ★ LLM 텍스트 -> {blocks, warnings}
 ├ loadProjectWikiSchemaRouting
 └ for 각 block:
    ├ sanitizeIngestedFileContent          [코드펜스/frontmatter:접두사/깨진 wikilink YAML 복구]
    ├ stampGeneratedLogDate / stampGeneratedFrontmatterDates
    ├ canonicalizeSourcesField
    ├ rewriteIngestPathFromTitleForTargetLanguage
    ├ validateWikiPageRouting -> 위반 시 drop(warning)
    ├ contentMatchesTargetLanguage -> 불일치 시 drop (concepts만; log/entities/sources 제외)
    └ 쓰기 분기: log.md=append / index,overview=overwrite / 그외=mergePageContent
```

### parseFileBlocks (라인 단위 상태머신, ingest.ts:357-450)
LLM 출력 형식:
```
---FILE: wiki/concepts/foo.md---
<본문>
---END FILE---
```
- 정규식: OPENER `ingest.ts:263` `/^---\s*FILE:\s*(.+?)\s*---\s*$/i`, CLOSER `ingest.ts:264`, FENCE `ingest.ts:331` `/^\s{0,3}(```+|~~~+)/`
- CRLF->LF 정규화 후 줄 split (프록시/서버 개행 방어)
- 코드펜스 상태머신: 펜스 안에서는 `---END FILE---`도 본문 취급(CommonMark, 같은 문자+길이 이상일 때만 종료). 포맷 설명 문서가 마커 인용해도 안 잘림.

### 블록 drop 4경로 (모두 warning 표면화)
- closer 없이 EOF -> truncation 의심 drop (ingest.ts:421)
- path 빈값 -> drop (ingest.ts:430)
- !isSafeIngestPath -> path-traversal drop (ingest.ts:437)
- 정상 -> blocks.push (ingest.ts:445)

### isSafeIngestPath (ingest.ts:291-307) — LLM 경로 위협모델 방어
- 제어/NUL 바이트 거부, 절대경로(/ \)·드라이브(C:)·UNC 거부
- 백슬래시 정규화 후 `..` 세그먼트 거부
- 세그먼트별 isWindowsSafePathSegment(ingest.ts:310): `<>:"|?*`·끝 공백/마침표·예약명(CON/PRN) 거부
- **wiki/ 하위 강제** (ingest 파이프라인 유일 쓰기 트리)

### hard failure vs soft drop (캐시 정합성 핵심)
- soft drop = 언어불일치/라우팅위반/traversal거부/빈path -> 의도된 결정, warnings에만
- hard failure = FS 거부(디스크풀/권한/OS) -> hardFailures push
- autoIngest 캐시: hardFailures 하나라도 있으면 ingest 캐시 안 씀 -> 다음 re-ingest 전체 재실행(부분결과 replay 방지)

### 병합 분기 mergePageContent (page-merge.ts, 본 트레이스 미독)
content 페이지는 기존 파일 있으면 clobber 대신 3단 병합:
1. frontmatter 배열(sources/tags/related) union
2. 본문 다르면 LLM 병합(buildPageMerger)
3. lock 필드(type/title/created) 기존값 강제, updated만 today
LLM 실패 시 "incoming 본문 + 배열 union" 폴백 + backupExistingPage

## feature-trace 메모: 페이지 병합 mergePageContent

진입점: writeFileBlocks의 content 페이지 분기(디스크에 같은 경로 존재 시) -> `src/lib/page-merge.ts:88` `mergePageContent(...)`. log.md(append)/index,overview(overwrite)는 안 탐.

### 핵심 설계: 결정론적 처리 + LLM은 본문만
frontmatter는 코드가 잠그고 LLM은 본문 통합만 담당. 3단계:
```
mergePageContent (page-merge.ts:88)
 ├ Fast path 1: existing 없음 -> newContent (신규)
 ├ Fast path 2: byte-identical -> existing (변화 없는 re-ingest)
 ├ Step 1: mergeArrayFieldsIntoContent -> sources/tags/related union (항상)
 ├ Fast path 3: 본문 동일(배열만 차이) -> arrayMerged, LLM 스킵
 ├ Step 2: merger() = LLM 본문 병합 (buildPageMerger, ingest.ts:2545)
 │    └ 실패/거부 시 -> arrayMerged 폴백 + tryBackup
 └ Step 3: 결정론적 후처리 -> LOCKED 복원 + 배열 재-union + updated=today
```

### LLM 병합 buildPageMerger (ingest.ts:2545)
streamChat(system=buildPageMergeSystemPrompt(), user=기존+신규, temp=0.1). 프롬프트 핵심(ingest.ts:2598-2623):
- 양쪽 모든 사실 보존(drop 금지), 중복만 제거
- subject/source 경계 보존(비교용 타 엔티티는 attribution-exact, 주제 주장으로 흡수 금지) — 커밋 1312525와 연결
- 충돌 주장은 분리+출처 명시, 단순 concat 아닌 논리적 재구성, [[wikilink]] 보존
- 첫 글자 `-`(--- 시작), frontmatter+body 전체. sources/tags/related/updated는 호출자가 덮어씀

### LLM 출력 거부(폴백) 3 sanity 게이트 — 걸리면 tryBackup 후 arrayMerged 폴백
- merger() throw (스트림 에러) -> page-merge.ts:117
- frontmatter 없음 -> page-merge.ts:130
- 본문 과소: llmBodyLen < max(old,new)*0.7 (truncation/게으른 요약) -> page-merge.ts:141
- BODY_SHRINK_THRESHOLD=0.7 (page-merge.ts:53)

### Step 3 결정론적 후처리 (LLM 신뢰 안 함, page-merge.ts:156-178)
- LOCKED_FIELDS=[type,title,created] (page-merge.ts:44) -> 기존값 강제 복원
- UNION_FIELDS=[sources,tags,related] (page-merge.ts:30) -> arrayMerged 기준 재-union(LLM이 부분집합만 echo해도 기여자 안 빠지게)
- updated -> today

### 부수효과
- 외부: streamChat 1회(본문 병합). Fast path 1/2/3은 LLM 호출 안 함(비용 절약)
- 디스크: 폴백/거부 시 backupExistingPage(tryBackup). 실제 writeFile은 호출자(writeFileBlocks)가 수행

## feature-trace 메모: frontmatter 파서 parseFrontmatter

공용 파서: wiki 페이지 문자열 -> {frontmatter, body, rawBlock}. `src/lib/frontmatter.ts:36`. mergePageContent sanity 3회, writeFileBlocks(canonicalizeSourcesField/라우팅), read-time 렌더링에서 광범위 사용.

### 콜 그래프
```
parseFrontmatter (frontmatter.ts:36)
 ├ locateFrontmatterBlock (frontmatter.ts:75)  ★ ---...--- 블록 위치 탐지
 │    -> 없으면 {frontmatter:null, body:content}
 ├ yaml.load(payload, JSON_SCHEMA)              [1차: 그대로]
 │    └ 실패 시 yaml.load(repairWikilinkLists(payload))  [2차: 수선 후 재시도]
 │         └ 또 실패 시 {frontmatter:null, body, rawBlock}
 └ normalize(parsed) (frontmatter.ts:179)       [모든 값 string|string[] 강제]
```

### locateFrontmatterBlock (2단계 매칭, frontmatter.ts:75-129)
- strict (frontmatter.ts:22) `^---\r?\n([\s\S]*?)\r?\n---` : 정상 케이스(파일 맨 앞)
- fallback (frontmatter.ts:34) `\n---\r?\n...` : 앞에 잡음(코드펜스 등) 낄 때. 단 여는 `---`가 처음 6줄 이내(MAX_PREFIX_LINES_BEFORE_FRONTMATTER=6, frontmatter.ts:35)여야 채택 -> 본문 깊숙한 `---` HR 오인 방지
- 방어 특수처리: fallback 원인이 ```yaml 펜스 opener면 body 머리의 닫는 펜스도 제거(frontmatter.ts:111-124). 안 하면 orphan ```로 body 전체가 코드블록 렌더됨(레거시 LLM-손상 페이지 호환)

### 2-pass YAML: repairWikilinkLists (frontmatter.ts:155-171)
LLM 비표준 YAML `related: [[a]], [[b]], [[c]]` -> 각 [[...]]를 따옴표로 감싸 `["[[a]]","[[b]]","[[c]]"]`. 이게 유일한 fixup. 그 이상 깨지면 frontmatter=null(추측 안 함). read-time 방어선(쓰기쪽 sanitizeIngestedFileContent와 역할 겹침).

### normalize (frontmatter.ts:179-190)
- 최상위 객체 아님/배열이면 null
- 배열 -> 각 요소 stringifyScalar -> string[]
- 스칼라 -> stringifyScalar
- 결과: 다운스트림은 항상 string|string[]만 봄(YAML 숫자/불리언/Date 타입 흔들림 제거). 그래서 mergePageContent LOCKED 비교가 typeof==="string"으로 단순.

### 설계 계약
파서 전반이 "복구는 정확히 1가지만, 나머지는 null 보고"라는 보수적 계약. frontmatter=null = "없음 또는 복구불가" 신호로 호출자가 분기.

## convention 메모: 신뢰 못 하는 입력 다루는 5대 방어 계약

LLM 출력/외부 파서/디스크 상태를 다루는 코드 전반의 반복 계약. queue->write->merge->frontmatter 4개 트레이스에서 일관 관찰. "낙관적으로 빠르게, 실패는 명시적으로, 추측은 절대 안 함".

### 계약 1 — 2-pass 복구: 정확히 1번만 고친다
깨진 입력은 알려진 손상 1개만 수선하고 재시도, 그 이상이면 실패 보고(추측 금지).
- parseFrontmatter: yaml.load 그대로 -> 실패 시 repairWikilinkLists 1회 -> 또 실패면 null (frontmatter.ts:48-57)
- parseFileBlocks: 펜스 상태머신으로 모호성 1개(코드펜스 내 마커)만 처리 (ingest.ts:391-405)
- 관례: 주석 "this is the only fixup we apply" 남김

### 계약 2 — Fast-path 사다리: 비싼 일(LLM) 전에 싼 탈출구부터
- mergePageContent: existing 없음 / byte-identical / 배열union 후 body 동일 -> 3관문 통과해야 streamChat (page-merge.ts:94-114)
- autoIngestImpl: checkIngestCache HIT면 전체 LLM 스킵 (ingest.ts:551)

### 계약 3 — 결정론적 후처리: LLM 출력을 그대로 믿지 않는다
LLM은 본문만, 구조 필드는 코드가 강제 덮어씀.
- mergePageContent Step3: LOCKED[type,title,created] 복원, UNION[sources,tags,related] 재union, updated=today (page-merge.ts:156-178)
- writeFileBlocks: stampGeneratedFrontmatterDates / canonicalizeSourcesField / rewriteIngestPathFromTitleForTargetLanguage (ingest.ts:1537-1560)

### 계약 4 — Warning 표면화 + soft/hard 실패 분류: 조용히 버리지 않는다
- 모든 드롭(truncation/빈path/traversal/언어불일치)을 warnings[] push -> activity 패널 (ingest.ts:421-443, 1632-1640)
- hard(FS거부)만 hardFailures[] -> 캐시 레이어가 키로 사용, 있으면 캐시 안 씀(부분결과 replay 방지) (ingest.ts:1525-1531)
- 큐: silent return [] 금지, throw해서 retry/failed 경로 (ingest.ts Step1/2 가드)

### 계약 5 — null/empty 보고: 모르면 모른다고 반환
- parseFrontmatter -> frontmatter:null (호출자가 폴백)
- isSafeIngestPath/isWindowsSafePathSegment -> false 반환, throw 안 함 (ingest.ts:291-307)
- tryReadFile/extractAndSaveSourceImages -> 실패 시 빈 값, ingest 중단 안 함

### 종합 한 줄
낙관적 fast-path -> 1회 한정 복구 -> 실패는 분류해 표면화 -> 구조 필드는 코드가 강제 -> 모르면 null.

### 새 코드 체크리스트
- [ ] LLM/네트워크 호출 앞에 fast-path 조기 반환?
- [ ] 파서 복구 1개 한정, 그 이상 null/[] 보고?
- [ ] LLM이 만든 구조 필드(날짜/경로/잠금메타)를 코드가 재세팅?
- [ ] 드롭/실패를 warnings push + soft/hard 구분?
- [ ] 비치명적 실패(이미지/캡션/캐시)는 throw 대신 graceful degrade?

## testing 메모: 방어 계약을 고정하는 테스트 구조

### 프레임워크
Vitest 4 (environment: node) + fast-check 4. 설정 vite.config.ts:47-53. 셋업 src/test-helpers/load-test-env.ts가 .env.test.local 로드(기존 키 보존 -> CI 오버라이드 허용). 컴포넌트는 .test.tsx.

### 테스트 종류 (suffix가 종류 결정)
- `*.test.ts` unit (순수함수, 모킹 streamChat) — 기본 실행 O
- `*.scenarios.test.ts` canned LLM 응답 순차 주입해 파이프라인 검증 — O
- `*.property.test.ts` fast-check 무작위 불변식 — O
- `*.integration.test.ts` / `*.race.test.ts` — O
- `*.real-llm.test.ts` 실제 LLM/임베딩 호출 — 기본 제외(별도)
- test:mocks = real-llm + mcp-server 제외 / test:llm = real-llm만 비병렬 (package.json:13-14)

### real-llm 게이팅
- `const ENABLED = process.env.RUN_LLM_TESTS === "1"` (ingest.real-llm.test.ts:42)
- `describe.skipIf(!ENABLED)` / `it.skipIf(!ENABLED)` 패턴
- 프로바이더 LLM_PROVIDER(ollama/minimax), 엔드포인트·모델 env. 픽스처 tests/fixtures/(gitignored), 타임아웃 15분
- `vi.mock("@/commands/fs", () => realFs)`로 fs만 실제 디스크

### scenarios 메커니즘 (ingest.scenarios.test.ts)
streamChat 모킹 -> stage1 분석/stage2 생성을 pendingResponses 큐에서 순차 emit. 시나리오는 @/test-helpers/scenarios/. 검증: 파일 존재+substring+review store 주입.

### 계약 <-> 테스트 1:1 매핑 (핵심)
- 계약1(파서 견고/1회복구) -> ingest-parse.test.ts: H1 CRLF(152), H3 tolerant marker(217), H5 code-fence(270-339, 3-tick는 4-tick 안 닫음)
- 계약2(fast-path) -> page-merge.test.ts:26-67 (null->new, byte-identical, bodies identical->LLM 스킵)
- 계약3(결정론적 후처리) -> page-merge.test.ts:111-137 (locked title/type 보존)
- 계약4(warning/soft·hard) -> ingest-parse.test.ts:186-385 (H2 truncated, H6 empty-path, traversal end-to-end) + ingest-queue.test.ts(MAX_RETRIES=3, empty array as failure)
- 계약5(null/empty 보고) -> page-merge.test.ts:139-211 (throw->폴백, body<70%->reject, no frontmatter->reject, backup 실패 삼킴) + isSafeIngestPath accept/reject 망라(ingest-parse.test.ts:385-471)

### property test 구조
`fc.assert(fc.property(arb, x => {...}))` round-trip 불변식. 예 extractJsonObject가 bare/fenced/trailing-prose JSON 동일 복원(sweep-reviews.property.test.ts:22-50). 기타 detect-language/path-utils/review-utils/review-store.

### 발견: CI가 테스트를 안 돌림 (중요)
.github/workflows/ci.yml은 vite build + cargo build만. 단위/시나리오/property 테스트 실행 단계 없음 -> 품질 게이트가 로컬(npm run test:mocks) 의존. PR에서 테스트 자동 검증 안 됨에 유의.

### 첫 기여 추천
순수함수+모킹 fs라 쉬운: frontmatter.test.ts / page-merge.test.ts / ingest-parse.test.ts. 실행 `npx vitest run <path>`, 전체 `npm run test:mocks`.

## history 메모: ingest 방어 계약이 막은 버그 (H1~H6 / isSafeIngestPath / hardFailures / page-merge)

각 계약은 실제 사용자 버그 리포트 또는 deep code review 발견에서 나옴. 약 8일(2026-04-22~04-30)간 집중 형성.

### 결정적 변경 3건
- `ed49e6e` (2026-04-22) 파서 견고성 H1~H6 — 실제 페이지 truncation 버그
- `7d413d5` (2026-04-25) 3대 치명 이슈 — path-traversal(보안)/캐시오염/동시성 race
- `6f4b7f0` (2026-04-30) 페이지 병합 — re-ingest 시 본문/태그 silent 손실

### H1~H6 기원: ed49e6e
발단: 사용자 리포트 — wiki/concepts/ingest-output-format.md가 551 bytes로 잘림. 근본원인: LLM이 코드펜스 예제에 리터럴 `---END FILE---` 인용 -> 순진한 정규식이 첫 occurrence에서 멈춰 나머지 silent 드롭.
-> parseFileBlocks 라인 스캐너로 교체:
- H1 CRLF->LF: 일부 Windows/프록시에서 전체 블록 드롭 수정
- H5 펜스 인식(CommonMark 길이매칭): 리포트된 truncation 직접 수정
- H3 관용적 마커 / H2 미닫힘 warning / H6 빈 path warning
이때 writeFileBlocks가 {writtenPaths,warnings} 반환 + activity 표면화 도입. 테스트가 진단용->회귀가드로 재작성. => 계약1·4 기원.

### 보안·정합성: 7d413d5 (deep code review 발견)
1. path-traversal(보안): LLM path 무검증 -> source 문서에 `../../../etc/passwd` 주입 가능. fs.rs::write_file은 자체 샌드박싱 없음(범용 커맨드). -> isSafeIngestPath() 신설. => 계약5 보안측면 기원.
2. 부분쓰기 캐시오염: 성공분만 saveIngestCache -> re-ingest가 부분결과 영원히 replay, 실패 페이지 silent 손실. -> hardFailures 분리, 있으면 캐시 skip. => 계약4 hard/soft 기원.
3. 동시성 race: index.md(read->overwrite), log.md(RMW). 큐는 자기 task만 직렬화하나 Save-to-Wiki/deep-research가 큐 우회해 autoIngest 직접 호출. -> project-mutex.ts withProjectLock 신설.

### 페이지 병합 3계층: 6f4b7f0
같은 엔티티 2번 ingest -> 이전 body/tags/related silent 드롭(sources 필드만 보호했음). 예: accumulibacter.md가 doc-B 본문만 남고 tags 소멸.
-> 3계층: (1)배열 union mergeArrayFieldsIntoContent (2)본문 LLM 병합+sanity(70%길이/frontmatter)+폴백백업 .llm-wiki/page-history/ (3)lock필드(type/title/created) 강제복원(title이동=wikilink깨짐, type이동=폴더재분류).
자기참조 버그: 배열 재적용이 existingContent 기준이라 LLM 누락 시 incoming 드롭 -> arrayMerged 기준으로 고침(page-merge.ts:171-178 "방어적 재-union" 주석 정체). => 계약2·3 기원.

### H 라벨 = 추적 장치
H1~H6은 ed49e6e의 개별 버그 케이스 번호(H=hypothesis/hardening 추정). 테스트 describe명(H1: CRLF 등)이 커밋 본문 H번호와 1:1. 커밋<->테스트<->코드주석이 같은 라벨 공유.

### 미해결
- 0677983(06-08 harden)은 커밋 본문 비어있음 — diff 봐야 함
- H4 결번 이유 미확인
- tests/page-merge-e2e/(gitignored) 13 shape 상세

## history 메모(후속): 미해결 의문 2건 해소

직전 history 메모의 "미해결" 항목 중 2건 답 확정.

### H4 결번 = 처음부터 존재한 적 없음 (삭제 아님)
- 현재 트리 전체 `\bH4\b` 검색 0건
- 도입 커밋 ed49e6e 원본 docstring부터 이미 H1,H2,H3,H5,H6 (H4 없음)
- 테스트 헤더가 명시: `silent-drop failure mode (H1/H2/H3/H5/H6)` (ingest-parse.test.ts:5), 코드주석도 동일(ingest.ts:339-352)
- 결론: 파서 리라이트 시 위험요소 번호 매기다 H4 가설이 재현 안 됨/다른 항목 흡수로 코드·테스트·커밋 어디에도 안 들어감. docstring이 "재현 가능한 것만" 번호 남기는 구조라 H4 자연 탈락. 원래 H4 내용은 흔적 없어 복구 불가(추정: H3/H5 중복 또는 비재현).

### 0677983 (06-08 harden) 정체 = aggregate repair 서브시스템 추가
커밋 본문은 비었지만 diff가 전부. 16파일 +541줄, ingest.ts +211줄로 집계 페이지(index/overview) 능동 복구 기능군 신설:
- aggregatePathsNeedingRepair (ingest.ts:1252): truncation으로 드롭된 집계 페이지 식별
- isAggregateRepairSafe (1286): 기존 파일 너무 크면 재생성 시 잘릴 위험 -> repair skip
- buildAggregateRepairPrompt (2028) / filterAggregateRepairOutput / aggregateRepairSectionCap
- sourceSummaryMediaRefsForExternalMarkdown / toSourceSummaryImageRef
연결: autoIngestImpl Step3 직후 집계 repair 분기(ingest.ts:952-954) = feature-trace 때 본 그 분기.
의미: 기존 H2(truncation)는 "warning 표면화"까지였는데, 이 커밋이 집계 페이지에 한해 LLM 재요청으로 능동 복구 추가. 단 기존 파일 크면 덮어쓰다 잘릴 위험 때문에 skip(계약1·4 연장: 복구하되 안전할 때만, 못 하면 warning).
동반: 같은 커밋이 scheduled-import(+45), codex_cli.rs, markdown-image-resolver, preset-resolver, i18n(en/zh) 동시 hardening. 제목 "harden ingest and scheduled import flows"와 일치.

### 남은 미해결
- tests/page-merge-e2e/(gitignored) 13 shape 상세 (여전히 미확인)

## 기능 메모: Deep Research 파이프라인 (codex 교차검증 완료)

LLM Wiki의 Deep Research = "검색 → LLM 합성 → 위키 저장 → 그래프 자동 편입"을 자동화하는 백그라운드 리서치 에이전트. 단일 책임 파이프라인이라 흐름이 명료함. 핵심은 `src/lib/deep-research.ts`.

### 데이터 흐름 (status: queued -> searching -> synthesizing -> saving -> done/error)
1. **진입 3경로** — 모두 `queueResearch()`로 수렴 (deep-research.ts:61)
   - 수동: ResearchPanel 입력 (research-panel.tsx:45) — topic 그대로 사용, 쿼리 최적화 없음
   - 그래프: knowledge gap 클릭 -> `optimizeResearchTopic`이 overview.md/purpose.md 읽고 LLM으로 topic+QUERY 3개 생성 (graph-view.tsx:858, optimize-research-topic.ts:14)
   - 리뷰: `__deep_research__` 액션, item.searchQueries 재사용 (review-view.tsx:56)
2. **큐/동시성** — `processQueue`가 maxConcurrent(기본 3, research-store.ts:34) 한도로 `executeResearch` 실행. queueResearch는 panelOpen 후 50ms 뒤 처리 시작 (deep-research.ts:77)
3. **소스 수집** `collectResearchSources` — 설정 `deepResearchSource`(기본 "web", AnyTXT 기본 off: wiki-store.ts:416-418)로 web/anytxt/both 분기
   - 웹: 쿼리당 5건, provider 5종(Tavily/SerpApi/SearXNG/Ollama/Firecrawl) 라우팅 (web-search.ts:153)
   - AnyTXT(로컬 파일 검색): 쿼리 전체를 한 번에 15건, LLM이 키워드로 rewrite 후 원본과 합쳐 검색 (anytxt-search.ts:114-115)
   - `Promise.allSettled` 병합 -> URL(또는 source:title:snippet) 기준 dedupe -> **최대 20건 캡**(MAX_RESEARCH_SOURCES, deep-research.ts:12,102)
   - 0건 + 실패 있으면 error, 0건 + 실패 없으면 "No research sources found." (noResearchSourcesTaskPatch:28)
4. **LLM 합성** — 소스를 `[N] title (source) snippet` 컨텍스트로, 기존 `wiki/index.md` 읽어 `[[wikilink]]` 교차참조 유도하는 system prompt로 `streamChat` 스트리밍. 토큰 누적분이 task.synthesis에 실시간 반영 -> 패널 마크다운 렌더 (deep-research.ts:222-268)
5. **저장** — `<think>/<thinking>` 블록 제거 후 frontmatter(`type: query`, `origin: deep-research`, `tags: [research]`) + 본문 + `## References`로 `wiki/queries/<makeQueryFileName>`에 writeFile (deep-research.ts:298-323)
6. **auto-ingest** — 저장 페이지를 `autoIngest(pp, fullPath, llmConfig)`로 비동기 편입(엔티티/개념/교차참조 추출). 트리 refresh + bumpDataVersion. 끝나면 100ms 뒤 `processQueue`로 다음 큐 (deep-research.ts:344,365)

### isActiveProjectPath 가드 패턴 (주의)
`executeResearch`는 거의 모든 단계 사이에 `isActiveProjectPath(pp)` / `updateTaskIfActive`로 "사용자가 그새 다른 프로젝트로 전환했나" 체크. 전환됐으면 조용히 return -> 백그라운드 task가 엉뚱한 프로젝트 store를 오염시키지 않게 하는 핵심 안전장치 (deep-research.ts:148-161).

### codex 교차검증 결과
codex(gpt-5.5, read-only)에 같은 질문 -> 11개 사안 전부 '사실' 판정, path:line 인용까지 정확. 과장/오류/확인불가 0건. 대상이 명료한 단일 파이프라인이라 이례적으로 깨끗했음.
